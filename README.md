* Create a bot using https://t.me/BotFather.
* Get your user id using https://t.me/userinfobot.
* Fill the `.env` based on the `.env.example`.
* Run `npm i` to install the dependencies.
* Run `npm run start` to start the bot.
